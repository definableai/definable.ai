Set `OPENAI_API_KEY` before you run a provider-backed agent.

- Mac and Linux: add `export OPENAI_API_KEY=sk-...` to your current shell or shell profile.
- Windows: use `setx OPENAI_API_KEY sk-...` for a persistent user-level variable.
