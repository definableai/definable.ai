---
title: Model Examples
sidebarTitle: Models
description: Curated, deterministic examples for the model layer.
icon: microchip
---

This page keeps the docs-facing model examples small and deterministic. The canonical script for this section is `examples/docs/models_basics.py`.

## Tested example

```python
from definable.model import Claude, OpenAIChat, resolve_model_string

openai = OpenAIChat(id="gpt-4o-mini")
claude = Claude(id="claude-sonnet-4-5-20250929", api_key="test-key")
resolved = resolve_model_string("openai/gpt-4o-mini")

assert openai.id == "gpt-4o-mini"
assert claude.id.startswith("claude")
assert type(resolved).__name__ == "OpenAIChat"
```

## Curated scripts

- `examples/docs/models_basics.py`: provider construction and model-string resolution
- `examples/docs/workflows_basics.py`: typed workflow execution downstream from model outputs

## Related pages

- [`Supported Models`](/models/overview)
- [`Structured Output`](/models/structured-output)
- [`Vision and Audio`](/models/vision-and-audio)
