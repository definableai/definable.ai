---
title: Reader Examples
sidebarTitle: Readers
description: Verified examples for local file parsing with BaseReader.
icon: file-lines
---

## Curated scripts

| Script | Covers |
| --- | --- |
| `python examples/docs/readers_basics.py` | Reading plain text files and checking parser selection |

## Tested reader surface

```python
from definable.media import File
from definable.reader import BaseReader


reader = BaseReader()
file = File(content=b"hello", filename="note.txt", mime_type="text/plain")
result = reader.read(file)
parser = reader.get_parser(file)

assert result.content == "hello"
assert type(parser).__name__ == "TextParser"
```

See [`Readers Overview`](/readers/overview) for the full API surface.
